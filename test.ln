-- variable declaration
val foo <- 4
mut bar: Int <- 4

-- functions
fun calc(~x, ~y: Int) -> Int {
  val z <- x / y
  z^2
}

calc(foo, bar)

fun say_hi(to person: String) {
  print("hi \(person)")
}

say_hi(to: "Stefan")
